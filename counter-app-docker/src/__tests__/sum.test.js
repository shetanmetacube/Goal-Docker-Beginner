
describe('more testing expression', () => {
    beforeEach(()=>?)
})

describe("global testing", ()=>{
    beforeEach(() => {
        console.log('Shaitan her')
    });

    it('sum the two number', () => {
        var value =  2 + 3;
        expect(value).toBe(5)
    })

})

describe('global testing', ()=>{
    
})

//collection of trimmed array

describe('more testing expression', () => {
    beforeEach(()=>?)
})

describe("global testing", ()=>{
    beforeEach(() => {
        console.log('Shaitan her')
    });

    it('sum the two number', () => {
        var value =  2 + 3;
        expect(value).toBe(5)
    })

})



describe('more testing expression', () => {
    beforeEach(()=>?)
})

describe("global testing", ()=>{
    beforeEach(() => {
        console.log('Shaitan her')
    });

    it('sum the two number', () => {
        var value =  2 + 3;
        expect(value).toBe(5)
    })

})